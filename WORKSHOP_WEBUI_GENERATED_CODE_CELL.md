# CaP-X Workshop WebUI 生成コード欄を空にしても消えないようにする

> 位置づけ: 変更記録（実装済み）。
> 前提: `WORKSHOP_WEBUI_SPEC.md` §3.1（Frontend）を読了済みであることを前提に書いている。動作確認は `WORKSHOP_WEBUI_LOCAL_DEV.md` のモックバックエンドで行う。

## 概要

プロンプトモードで「生成」すると、LLM 出力から抽出したコードがコード欄（`Cell`）に入り、手で編集できる。ところがコード欄の中身を**全部消すとコード欄そのものが消え**、`リセット&Run` ボタンも一緒に消える。一度消えると、もう一度「生成」する以外に戻す手段がない。

## 原因

`PromptExperimentPanel.tsx` がコード欄を条件付きで描画している。

```tsx
{activeTurn.extractedCode && (
  <Cell … onChange={(code) => onUpdateTurnCode(lastIdx, code)} … />
)}
```

`extractedCode` は「生成前でまだコードがない」と「生成後にユーザーが空にした」の両方で `""` になり、この条件は両者を区別できない。設計意図は前者（生成前はコード欄を出さない）だけで、後者は副作用。エディタで全文を消すと `onChange("")` → `extractedCode = ""` → `Cell` がアンマウントされる。

モック＋ Chromium で再現済み: 生成直後はコード欄あり（Monaco エディタ1つ、`リセット&Run` あり）、`setValue("")` 後はコード欄なし（エディタ0、`リセット&Run` なし）。`LLM出力` の折りたたみは残る。

## 方針

「コード欄を出すかどうか」の判定を、`extractedCode` の中身ではなく**生成が完了したかどうか**にする。生成が完了すると `llmRawResponse`（LLM 出力の全文）が埋まるので、これを条件にする。空にしてもコード欄は残る。

厳密には「LLM が1文字も返さずに正常完了した」場合だけ `llmRawResponse` が `""` のままになり（`app.py` の `generate_experiment` は `delta` が0件でも `full_text = ""` で `done` を返す）、コード欄は出ない。これは修正前と同じ状態で、参加者は「生成」をやり直せば済む。稀なケースのために完了フラグを保存形式に足すことはせず、許容する。

空のコードは実行させない。`リセット&Run` はコードが空白のみのとき無効にする。

## 変更内容

- `workshop/webui/src/components/PromptExperimentPanel.tsx`: コード欄の描画条件を `activeTurn.extractedCode` から `activeTurn.llmRawResponse` に変更。`Cell` に `disableWhenEmpty` を渡す
- `workshop/webui/src/components/Cell.tsx`: `disableWhenEmpty?: boolean` を追加。真のとき、コードが空白のみ（`trim() === ""`）なら `▶ Run` / `リセット&Run` を無効にする。既定は偽で、手動モードのセルは従来通り

## 設計判断

- **条件は `llmRawResponse`。** 「生成が完了した」ことを表す既存の値で、新しい状態を足す必要がない。`extractedCode` はユーザーが自由に書き換える値なので、表示の有無をそれに依存させない
- **空のときは `リセット&Run` を無効にする。** 空コードを送っても本物のバックエンドは空文字を `exec()` するだけで害はないが、押せても意味がなく、押した参加者が「何も起きない」と戸惑う。無効化して「コードを書く必要がある」ことを示す
- **手動モードのセルは変えない。** 手動モードでは空セルを `Run All` で通り過ぎる使い方があり、空セルの `Run` を無効にする理由がない。`Cell` は共通コンポーネントなので、無効化はプロパティで opt-in にし、プロンプトモードからだけ渡す
- **保存形式は変えない。** `notebooks.ts` の `PromptTurn` はそのまま。保存済みノートブックを開いたときも同じ条件（`llmRawResponse` の有無）で描画されるので、過去のデータにも同じ修正が効く
- **完了時のスクロール（`WORKSHOP_WEBUI_LLM_OUTPUT.md`）は `extractedCode` を見たまま。** 抽出結果が空になる生成（空白のみの応答、中身が空のコードフェンス）では、空のコード欄は出るがそこへスクロールはしない。判定の目的が違う（「見せる」と「移動する」）ので揃えない。なおフェンスのない応答は `code_extract.py` が全文を `strip()` してコードとして返すため、空にはならない

## 影響範囲

- プロンプトモードのコード欄の表示条件と、`リセット&Run` の有効条件だけ
- 応答が空白のみ、または中身が空のコードフェンスで `extractedCode` が空のまま完了した場合、従来はコード欄が出なかったが、修正後は空のコード欄（実行ボタン無効）が出る。参加者が手でコードを書けるようになるので、改善として受け入れる
- 応答が完全な空文字（`""`）で完了した場合は、修正前と同じくコード欄が出ない（「方針」参照）
- 手動モードには影響しない

## テスト結果

モックバックエンド＋ Chromium で確認。

| 操作 | コード欄 | `リセット&Run` |
|---|---|---|
| 生成直後（8行） | あり | 有効 |
| 全文削除 | **残る** | 無効 |
| 改行と空白だけ | 残る | 無効 |
| コードを書いて実行 | 残る | 有効 |

- 手動モードの空セルは `▶ Run` / `リセット&Run` / `リセット&ここまでRun` すべて有効のまま（変更なし）
- `tsc -b` と `npm run build` が通る

## 備考

- `package.json` への追加はなし

## 追記: コードレビュー（Codex）

実装後に Codex CLI でレビューし、2点を反映した。いずれも実装は変えず、ドキュメントの記述を正した。

- 「生成が完了すると `llmRawResponse` が必ず埋まる」は保証されていない（空文字で正常完了しうる）。完了フラグを保存形式に足す案もあったが、稀なケースで再生成すれば済むため許容し、「方針」にその旨を明記した
- 「フェンスなし応答では `extractedCode` が空」は誤り。`code_extract.py` はフェンスがなければ全文を返す。空になる条件を「空白のみの応答」「中身が空のフェンス」に訂正した

手動モード・Self-Refine・完了時スクロール・保存形式への副作用、`disableWhenEmpty` の適用範囲（`リセット&ここまでRun` は手動モード専用で対象外）は「問題なし」だった。
