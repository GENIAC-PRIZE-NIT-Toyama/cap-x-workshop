# CaP-X Workshop WebUI LLM出力の枠を文量に合わせて伸ばし、ストリーミングに画面を追従させる

> 位置づけ: 変更記録（実装済み）。
> 前提: `WORKSHOP_WEBUI_SPEC.md` §3.1（Frontend）を読了済みであることを前提に書いている。動作確認は `WORKSHOP_WEBUI_LOCAL_DEV.md` のモックバックエンドで行った。

## 概要

プロンプトモードで「生成」を押すと、LLM の出力が `<details class="llm-output">` の中の `<pre class="prompt-text-readonly">` にストリーミングで流し込まれる（`PromptExperimentPanel.tsx`）。この `pre` は `max-height: 260px` + `overflow-y: auto` で高さが固定されていたため、260px を超えた分は枠の中でスクロールしないと見えず、ストリーミング中は新しい文字が枠の下端の外に流れていって視界に入らなかった。

枠を文量に合わせて伸ばし、ストリーミング中はエディタペインを自動でスクロールして最新の文字が常に見えるようにした。

## 変更内容

- `workshop/webui/src/styles.css`: `.prompt-text-readonly` の `max-height: 260px` と `overflow-y: auto` を外す。枠は内容の高さになる
- `workshop/webui/src/components/PromptExperimentPanel.tsx`: ストリーミング中の `pre` に `ref` を持たせ、`streamingText` が増えるたびに、その `pre` の下端がスクロール容器（最も近い `.pane`）の表示領域の下端に来るようスクロールする。ユーザーが上にスクロールして末尾から離れたら追従を止め、末尾近くまで戻ってきたら再開する
- `workshop/webui/mock-server/server.js` / `fixtures.js`: プロンプトに `[long]` を含めると長い応答（数十行）を返す。この変更の確認用（`WORKSHOP_WEBUI_LOCAL_DEV.md` §4）

## 設計判断

- **追従はストリーミング中だけ。** 生成が終われば普通に手動スクロール。完了後の `LLM出力` は従来通り折りたたまれる（`open={ui.generating}`）ので、長文が画面を占有し続けることはない
- **ユーザーが上にスクロールしたら追従を止める。** 途中まで出た文章を読み返そうとしたのに末尾へ引き戻される、を防ぐ。判定は「`pre` の下端が表示領域の下端からどれだけ離れているか」で、しきい値（40px）以内なら追従、超えたら停止。プログラムによるスクロールは常にしきい値内に収まるので、ユーザーのスクロールだけが追従を止める。末尾近くまで戻れば再開する
- **スクロール容器は `closest(".pane")` で探す。** 画面は `100vh` 固定で、スクロールするのはウィンドウではなく中央の `.pane-editor`（`styles.css` の `.pane { overflow-y: auto }`）。`PromptExperimentPanel` はペインを所有していないので、DOM を辿って見つける。`window.scrollTo` では動かない
- **容器の末尾ではなく `pre` の下端に合わせる。** 容器の末尾（`scrollHeight`）に張り付けると、下に別の実験や「+ 実験を追加」ボタンがある場合にストリーミング中の出力を通り過ぎてしまう
- **`max-height` は履歴側も外す。** `.prompt-text-readonly` は完了したターンの履歴（「送信したプロンプト」「LLMの出力」）でも使われている。同じ出力がアクティブなときは全文、履歴になると 260px スクロール、では一貫しない。履歴は折りたたみの中なので、明示的に開かない限り長文は表示されない
- **確認用にモックへ長い応答を足す。** 既定の応答は10行程度で 260px に収まってしまい、伸縮も追従も検証できない。プロンプトに `[long]` を含めたときだけ長い応答を返すようにし、既定の挙動は変えない

## 影響範囲

- 表示のみ。送信されるプロンプト・受信する出力の内容は変わらない
- 手動モードには影響しない
- 履歴の「送信したプロンプト」「LLMの出力」を開いたときの高さが内容に応じて変わる（従来は 260px 上限）

## テスト結果

モックバックエンド＋ Chromium で確認。

- `[long]` を含むプロンプトで生成すると、`LLM出力` の枠が文量に合わせて伸び、枠内スクロールバーは出ない
- ストリーミング中、`.pane-editor` が自動でスクロールし、`pre` の下端が常に表示領域内にある
- ストリーミング中に上へスクロールすると追従が止まり、そのまま末尾へ引き戻されない。末尾近くまで戻すと追従が再開する
- 生成完了後は `LLM出力` が折りたたまれ、追従も止まる
- 既定の短い応答では従来と見た目が変わらない
- `tsc -b` と `npm run build` が通る

## 備考

- `package.json` への追加はなし
