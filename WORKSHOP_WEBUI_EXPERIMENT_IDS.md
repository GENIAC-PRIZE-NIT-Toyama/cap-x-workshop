# CaP-X Workshop WebUI 保存済みノートブックを開いた後の実験ID衝突を防ぐ

> 位置づけ: 変更記録（実装済み）。
> 前提: `WORKSHOP_WEBUI_SPEC.md` §3.1（Frontend）を読了済みであることを前提に書いている。動作確認は `WORKSHOP_WEBUI_LOCAL_DEV.md` のモックバックエンドで行う。

## 概要

プロンプトモードで実験が複数あるとき、片方のプロンプト欄に入力した文字が**別の実験のプロンプト欄にも入る**。「生成」を押さなくても起きる。生成結果も同様に、別の実験へ書き込まれる。

## 原因

実験の ID は `exp-<連番>` で、連番はモジュール変数 `experimentCounter`（`App.tsx`）から振る。この ID はノートブックと一緒に localStorage へ**保存される**が、カウンタはページを読み込むたびに 0 に戻る。

1. 実験を 2 つ作って保存する（`exp-1`, `exp-2`）
2. ページを再読み込みして、保存済みノートブックを開く（保存された `exp-1`, `exp-2` がそのまま使われる。カウンタは 0 のまま）
3. 「実験を追加」する → `newExperiment()` が `exp-1` を振る。既存の `exp-1` と同じ ID になる

`updateTurn` / `updateExpUi` は `exp.id` が一致する実験をすべて更新する（`prev.map((e) => (e.id === expId ? … : e))`）。ID が重複していると、片方への入力・生成が同じ ID の実験すべてに反映される。重複した状態でそのまま自動保存されるため、以後そのノートブックを開くたびに再発する。

## 方針

ID はそのページ内で一意であればよく、ページをまたいで意味を持たない（React の `key` と実験ごとの UI 状態 `expUi` のキーにしか使わない）。そこで、保存済みノートブックを開くときに ID を**振り直す**。新規作成と同じカウンタから振るので、以後に追加する実験とも衝突しない。

## 変更内容

- `workshop/webui/src/App.tsx`: `withFreshId(exp)` を追加し、保存済みノートブックを開くとき（`experiments` の復元）に全実験へ適用する

## 設計判断

- **保存時ではなく読み込み時に直す。** すでに重複 ID で保存されてしまったノートブックも、開くだけで直る。保存形式は変えない
- **ID を UUID にはしない。** 既存の採番（`newExperiment`）を変えずに済み、変更が小さい。保存された ID を使い続けないことが本質で、採番方法の問題ではない
- **手動モードのセルは対象外。** セルは保存時にコードの文字列だけを保存し、開くたびに `newCell()` で ID を新しく振るため、同じ問題は起きない

## 影響範囲

- 保存済みのプロンプトモードのノートブックを開いたときの実験 ID のみ。保存形式・送信内容・バックエンドには影響しない
- 実験 ID は永続化された値としてはどこからも参照されない（`expUi` のキーと `key` 属性のみ）

## テスト結果

モックバックエンド + ヘッドレス Firefox で確認（プロンプトモード、2 実験）。

| 手順 | 修正前 | 修正後 |
|---|---|---|
| 2 実験を作り、`AAA-first` / `BBB-second` と入力 → 再読み込み → 保存済みノートブックを開く → 「実験を追加」→ 3 つ目に `CCC-third` と入力 | `['CCC-third', 'BBB-second', 'CCC-third']`（1 つ目が上書きされる） | `['AAA-first', 'BBB-second', 'CCC-third']` |
| 上と同じ手順。ただし保存データの実験 ID を全部 `exp-1` に書き換えてから開く（すでに壊れた保存データ） | 未確認（衝突が確定しているため） | `['AAA-first', 'BBB-second', 'CCC-third']` |

`npm run build` は成功。
