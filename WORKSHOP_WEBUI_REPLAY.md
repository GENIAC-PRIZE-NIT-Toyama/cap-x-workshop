## 概要
WebUIのUX向上のため、クライアントサイドでのリプレイ再生機能の追加と、エディタ周りのテーマ連動・シンタックスハイライト対応を行いました。

## 変更内容
- 軽量な自動リプレイ機能の追加
  - サーバーにMP4をエンコードさせる負荷を回避するため、WebSocketから受け取った画像配列をクライアント側で連続再生するプレイヤーを実装しました。
  - 再生/一時停止、シークバー、コマ送り（`❘◀` / `▶❘`）のコントロールUIを追加。
  - ループ再生時、最後のフレームで1秒間待機してから最初に戻るように調整し、動作を確認しやすくしました。
  - 補足
    - デフォルトコマ送り時間は150ms (6.7fps) としています。`CameraView.tsx`→`ReplayPlayer`→`delay`で変更可能
    - 長時間実行時にバッファが重くなる可能性があります。
    - 環境リセットからのすべての動作をリプレイします。

- APIドキュメントのシンタックスハイライト対応
  - `ApiDocsModal` のテキスト描画を `<pre>` タグから `Monaco Editor (Read-only)` に差し替え、Python関数のシグネチャをハイライトされるようにしました。
  - Monaco EditorにするにあたりいくつかCSSを変更しました。

- ダークモード/ライトモードの完全連動
  - アプリ全体のテーマ切り替え時、APIドキュメントモーダルだけでなく、メインのセルエディタ `Cell.tsx` の背景色とテーマも即座に連動して切り替わるように修正しました。

## 備考
- `package.json` への新規ライブラリ追加はありません（既存の `@monaco-editor/react` を流用）。
- サーバーサイドの処理は一切変更していないため、バックエンドの負荷増大はありません。

---

## 前回のPR (feat: Add cell action buttons, theme switch #2) での変更点
すでにマージされた内容を併せて以下に記録します。主にセル操作の拡充を行いました。

- セル操作のColab風UI化
  - セルとセルの間にホバーで出現する追加ボタン（`.cell-divider`）を実装し、任意の位置にセルを挿入可能にしました。
- 実行制御の細分化
  - エディタ上部に固定ヘッダーを追加し、`Run All` や `環境リセット & Run All` を配置しました。
  - 各セル単位での `リセット&Run` `リセット&ここまでRun` ボタンを追加し、途中からのデバッグを容易にしました。
- その他
  - アプリ全体にダーク/ライトモードの切り替えトグルを実装しました。(`Toolbar.tsx`→`onToggleTheme`)
